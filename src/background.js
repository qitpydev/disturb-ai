// background.js - Chrome extension service worker

// Placeholder for monitoring tab activity and detecting distractions.

chrome.runtime.onInstalled.addListener(() => {
  console.log('Disturb AI extension installed');
});
